# Plans

Keep active and completed execution plans in this directory so agents can work from repository-local context instead of relying on external chat history.

Suggested layout:

```text
docs/plans/
├── active/
└── completed/
```
