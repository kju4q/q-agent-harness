name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Check markdown links (relative paths must resolve)
        uses: lycheeverse/lychee-action@v2
        with:
          args: --offline --no-progress --include-fragments './**/*.md'
          fail: true

      - name: Shellcheck demo scripts
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: ./demo

      # scripts/compact is Python and has a python3 shebang, so shellcheck
      # skips it. It is covered by the syntax check below instead.
      - name: Shellcheck scripts
        uses: ludeeus/action-shellcheck@master
        with:
          scandir: ./scripts

      - name: Node syntax check (demo/schema-validation.js)
        run: node --check demo/schema-validation.js

      - name: Python syntax check (scripts/compact)
        run: python3 -m py_compile scripts/compact
